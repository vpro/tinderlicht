var config = require( './../../config.json' );
var express = require('express');
var Firebase = require('firebase');
var router = express.Router();

// TODO: looks unused in the front end code

module.exports = router;